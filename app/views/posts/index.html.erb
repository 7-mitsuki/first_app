<%# rails g contoller postsで、app/views/配下に、
    postsコントローラ用のviewフォルダのpostsが生成される。 %>

<%# postsコントローラに対して、indexアクションを行った際に、返されるされるビュー %>

<h1>トップページ</h1>

<%= link_to '新規投稿', '/posts/new' %>

<% @posts.each do |post| %>
  <div class="post">
    <div class="post-date">
      投稿日時：<%= post.created_at %>
    </div>
    <div class="post-content">
      <%= post.content %>
    </div>
  </div>
<% end %>